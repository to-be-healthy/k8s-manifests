apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tobehealthy

resources:
  - 00-namespace.yaml
  - 20-mysql.yaml
  - 21-redis.yaml
  - 30-backend.yaml
  - 31-frontend.yaml
  - 40-ingress.yaml

# ClusterIssuers are cluster-scoped; apply once via bootstrap, not part of namespaced kustomization.
